en:
  activerecord:
    errors:
      models:
        guess:
          attributes:
            code_pegs:
              more_than_four: "can't be more than four"
              less_than_four: "can't be less than four"
              invalid_colours: "can't include invalid colours"
  helpers:
      submit:
        guess:
          create: Submit
        game:
          create: New game
